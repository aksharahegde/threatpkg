DATABASE_URL=
REDIS_URL=
GITHUB_TOKEN=
# Manual POST /api/sync (header x-sync-secret)
SYNC_SECRET=
# Vercel Cron: set on Vercel; cron hits GET /api/cron/sync with Authorization: Bearer <CRON_SECRET>
CRON_SECRET=
OPENAI_API_KEY=
# Canonical public URL (must match your custom domain in production for OG/social previews)
NUXT_PUBLIC_SITE_URL=http://localhost:3000
